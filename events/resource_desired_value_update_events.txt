namespace = desired_resource_events

country_event = {
	id = desired_resource_events.1
	hide_window = yes
	is_triggered_only = yes

	immediate = {
		country_event = { id =  zro_huffing_desired_resource_events.1 }
	}
}